name: fluttertest
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  provider: ^4.3.2
  objectdeliverer_dart: ^0.0.8
  shared_preferences: ^0.5.8
  
dev_dependencies:
  pedantic: ^1.9.0

  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
# /で終わるとディレクトリの中身を全部追加
    - images/
